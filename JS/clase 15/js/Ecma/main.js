console.log('hello world')
let data = () =>{
    document.addEventListener('keypress', (event)=>{
        event.preventDefault
        event === 'k' ? console.log(`Estas presionando la tecla ${event}`) :console.error('algo salio mal')

    } )
}